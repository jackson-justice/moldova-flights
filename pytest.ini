[pytest]
testpaths = tests
# Disable the globally-installed BYU autograder plugin: it keys on a fixed
# registry of test node IDs and raises KeyError during collection for any test
# not in that registry, which breaks this project's test runs entirely.
addopts = -p no:byu_pytest_utils
